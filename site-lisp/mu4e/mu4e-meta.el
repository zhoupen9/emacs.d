;; auto-generated
(defconst mu4e-mu-version "1.4.13"
  "Required mu binary version; mu4e's version must agree with this.")

(defconst mu4e-builddir "/build/maildir-utils/parts/mu/build"
  "Top-level build directory.")

(defconst mu4e-doc-dir "/share/doc/mu"
  "Mu4e's data-dir.")

(provide 'mu4e-meta)
